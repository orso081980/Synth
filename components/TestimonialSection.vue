<template>
  <section class="bg-[#161616] flex justify-center pb-[8rem]">
    <div class="relative overflow-hidden z-0 testimonial-card max-w-6xl mx-auto px-6 grid grid-cols-1 md:grid-cols-3 gap-8 border border-[#636363] py-14">
      <!-- Left Column (1/3) -->
      <div class="flex flex-col items-center text-center">
        <img :src="imageSrc" :alt="personName" class="mb-4 w-auto max-w-[150px]" />
        <h4 class="text-xl font-semibold text-white">{{ personName }}</h4>
        <p class="text-gray-300 mt-2">{{ personTitle }}</p>
      </div>
      <!-- Right Column (2/3) -->
      <div class="md:col-span-2 flex flex-col justify-center">
        <i class="text-6xl text-[#A620FF] fa-solid fa-quote-left"></i>
        <h3 class="text-2xl font-bold text-white mt-4 mb-6">{{ quoteTitle }}</h3>
        <p class="text-gray-300">
          "{{ quoteText }}"
        </p>
        <a :href="ctaLink" class="mt-6 w-auto max-w-max px-4 py-2 button btn-2 text-white font-semibold">
          <span>{{ ctaText }}</span>
        </a>
      </div>
    </div>
  </section>
</template>

<script setup>
defineProps({
  imageSrc: String,
  personName: String,
  personTitle: String,
  quoteTitle: String,
  quoteText: String,
  ctaText: String,
  ctaLink: String
});
</script>
